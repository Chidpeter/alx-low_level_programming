#include "main.h"

/**
 * print_most_numbers - Print numbers without 2 and 4
 */

void print_most_numbers(void)
{
	int a;

	for (a = 48; a < 58; a++)
	{
	if (a == 50 || a == 50)
	{
	continue;
	}
	_putchar(a);
	}
	_putchar('\n');
}
